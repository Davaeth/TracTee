CREATE TABLE timer (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    time REAL NOT NULL,
    title TEXT
);

insertTimer:
INSERT INTO timer (time, title)
VALUES (?, ?);

getNewestTimer:
SELECT *
FROM timer
ORDER BY id DESC
LIMIT 1;

selectAll:
SELECT *
FROM timer;